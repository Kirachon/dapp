// Deprecated: NextAuth middleware removed. SuperTokens protects API and GraphQL.
export function middleware() {
  // No-op; rely on client-side AuthContext and server-side GraphQL session validation
}

